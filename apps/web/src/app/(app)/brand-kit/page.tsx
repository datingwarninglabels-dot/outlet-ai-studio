import { PlannedSection } from "../planned-section";

export default function BrandKitPage() {
  return (
    <PlannedSection
      title="Brand Kit"
      description="Logo, colors, fonts, intro/outro, and default style settings ship in milestone M7."
    />
  );
}
