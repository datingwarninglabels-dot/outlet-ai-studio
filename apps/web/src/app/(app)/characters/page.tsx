import { PlannedSection } from "../planned-section";

export default function CharactersPage() {
  return (
    <PlannedSection
      title="Character Library"
      description="Consistent-character creation, reference uploads, and character sheets ship in milestone M3."
    />
  );
}
