import { PlannedSection } from "../planned-section";

export default function ProjectsPage() {
  return (
    <PlannedSection
      title="Projects"
      description="Project creation, autosave, and resume will land alongside the first Create Video milestone."
    />
  );
}
