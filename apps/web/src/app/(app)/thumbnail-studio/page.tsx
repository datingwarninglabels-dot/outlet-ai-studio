import { PlannedSection } from "../planned-section";

export default function ThumbnailStudioPage() {
  return (
    <PlannedSection
      title="Thumbnail Studio"
      description="Thumbnail and cover generation, editable headline text, and platform export presets ship in milestone M7."
    />
  );
}
