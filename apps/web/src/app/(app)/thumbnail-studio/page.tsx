import { PlannedSection } from "../planned-section";

export default function ThumbnailStudioPage() {
  return (
    <PlannedSection
      title="Thumbnail Studio"
      description="Thumbnail generation, style options, and editable headline text are already available from within each project — open a project and generate thumbnails there. A standalone hub for browsing thumbnails across every project isn't built yet."
    />
  );
}
